You are filling dropdown fields on a job application form for {name}. Choose the best value for EACH field listed below, grounded ONLY in the applicant facts provided. Never invent employers, job titles, dates, degrees, certifications, or any credential the applicant does not have.

Applicant facts:
{profile_block}

Role being applied to: {job_title} at {company}

Pre-written screening answers (BACKGROUND CONTEXT for your reasoning ONLY -- these are prose written for open-text questions; NEVER copy this prose into a value. Every field below is a dropdown, so derive the short option the prose implies, e.g. a "Yes, I have 4+ years..." answer means the dropdown value is "Yes"):
{screening_answers}

Dropdown fields to decide (each has a key, a label, whether it is required, and -- when known -- the allowed options):
{fields_json}

Call submit_fill_plan exactly once, with one entry per field key above:
- value: the exact option to select -- a short, selectable token, NOT a sentence. If options are listed for that field, value MUST be exactly one of them. If options are NOT listed (shown as null, e.g. a yes/no screener whose choices load on click), value MUST still be the short on-page choice you expect to pick ("Yes", "No", "5+", "Bachelor's degree"), never a paragraph or explanation. A value longer than a few words is WRONG -- the form select would never match it. The reasoning goes in the reasoning field, never in value.
- reasoning: one sentence naming the specific applicant fact that supports the value.
- confidence: a number from 0.0 to 1.0.
- grounded: true ONLY when a real applicant fact supports the value. Set it to false when you would be guessing, when the facts do not support any answer, or when you are unsure -- the human will fill that field instead. Filling fewer fields correctly is better than guessing.

Demographic (EEO), legal-consent, and identity questions are deliberately NOT in this list. Do not answer anything outside the listed keys.
